[program:sshd]
command = /bin/bash -c "env > /etc/environment && /usr/sbin/sshd -D"
autostart = true

[program:cron]
command = cron -f -L 15
autostart = true
autorestart = true



























